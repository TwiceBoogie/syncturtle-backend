server:
  port: 8001
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/user
    username: sebastian
    password: Twice_Mina1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: false
    properties:
      hibernate:
        temp:
          "[use_jdbc_metadata_defaults]": false
  liquibase:
    enabled: true
    user: sebastian
    password: Twice_Mina1
    change-log: classpath:db/changelog/db.changelog-master.xml
  rabbitmq:
    addresses: amqp://sebastian:Twice_Mina1@${RABBITMQ_HOST:localhost}

rabbitmq:
  exchanges:
    internal: internal.exchange
  queues:
    mail: mail.queue
  routing-keys:
    internal-mail: internal.mail.routing-key