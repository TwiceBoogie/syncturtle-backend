server:
  port: 8020
spring:
  application:
    name: email-service
  rabbitmq:
    addresses: amqp://email-service:${EMAIL_SERVICE_PW:6cct9rVawlKtBkDwoE2ITl3niIU}@${RABBITMQ_HOST:localhost}
  mail:
    host: smtp.gmail.com
    username: salsebastian13@gmail.com
    password: dwgx trqd nebn ecsf
    port: 465
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
mail:
  debug: true

rabbitmq:
  exchanges:
    internal-direct: email.exchange
    internal-fanout: user.events
  routing-keys:
    internal-mail: high_priority.mail
  queues:
    internal-fanout-queue: q.usersvc
    internal-mail: q.mail