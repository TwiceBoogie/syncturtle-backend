server:
  port: 8020
spring:
  application:
    name: email-service
  cloud:
    vault:
      scheme: http
      host: localhost
      port: 8200
      uri: http://localhost:8200
      authentication: TOKEN
      token: ${VAULT_TOKEN:057287df061af3f4ec3f3f1d6b353b1c6c3becc5350ff713a3d601d25e2f1f88}
      fail-fast: true
      config.lifecycle.enabled: true
      rabbitmq:
        enabled: true
        role: my-role
        backend: rabbitmq
        username-property: spring.rabbitmq.username
        password-property: spring.rabbitmq.password
  mail:
    host: smtp.gmail.com
    username: salsebastian13@gmail.com
    password: dwgx trqd nebn ecsf
    port: 465
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
mail:
  debug: true

rabbitmq:
  exchanges:
    internal-direct: email.exchange
    internal-fanout: user.events
  routing-keys:
    internal-mail: high_priority.mail
  queues:
    internal-fanout-queue: q.usersvc
    internal-mail: q.mail