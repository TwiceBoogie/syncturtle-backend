server:
  port: 8002
spring:
  application:
    name: password-service
  cloud:
    vault:
      scheme: http
      host: localhost
      port: 8200
      uri: http://localhost:8200
      authentication: TOKEN
      token: hvs.azfjWWDF3WcBSKYaAJCzugW8
      fail-fast: true
      config.lifecycle.enabled: true
      database:
        enabled: true
        role: personavault-readonly
        backend: database
        username-property: spring.datasource.username
        password-property: spring.datasource.password
  config:
    import: vault://
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: false
    properties:
      hibernate:
        temp:
          "[use_jdbc_metadata_defaults]": false
  rabbitmq:
    addresses: amqp://sebastian:Twice_Mina1@${RABBITMQ_HOST:localhost}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
logging:
  level:
    root: INFO
    "[org.springframework.web]": DEBUG
    "[org.springframework.cloud.vault]": DEBUG

rabbitmq:
  exchanges:
    internal-direct: direct.exchange
    internal-fanout: fanout.exchange
  routing-keys:
    internal-created: user.created
    internal-deleted: user.deleted
    internal-mail: user.mail-token
  queues:
    internal-fanout-queue: q.fanout-user-created
    internal-created: q.user-created
    internal-deleted: q.user-deleted
    internal-mail: q.mail