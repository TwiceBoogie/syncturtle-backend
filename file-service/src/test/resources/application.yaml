server:
  port: 0

spring:
  application:
    name: file-service

  main:
    banner-mode: "off"

  cloud:
    config:
      enabled: false
  config:
    import: ""

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

app:
  s3:
    enabled: false

amazon:
  aws:
    access-key: ""
    secret-key: ""
    region: us-east-1
  s3:
    bucket:
      task:
        name: taskattachment
      user:
        name: spidergwen

logging:
  level:
    root: WARN
